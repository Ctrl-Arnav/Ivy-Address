# Adaptive Privacy Observatory — Backend Dependencies (Phase 2)
# ML dependencies (transformers, torch, etc.) will be added in Phase 3.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0,<14.0
